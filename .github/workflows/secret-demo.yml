name: Secrets Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: 🔐 Access Secret
        run: 'echo "My secret is: ${{ secrets.DEMO_SECRET }}"'
      
      - name: 🔒 Masked output
        run: |
          echo "Secret value will be masked in logs automatically"